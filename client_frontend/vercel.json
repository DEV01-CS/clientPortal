{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "installCommand": "rm -f ~/.npmrc /root/.npmrc .npmrc 2>/dev/null || true && npm config set registry https://registry.npmjs.org/ && npm config delete @scope:registry 2>/dev/null || true && npm config set @scope:registry https://registry.npmjs.org/ && npm install",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

