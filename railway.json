{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd client_frontend && npm config set registry https://registry.npmjs.org/ && npm ci --registry=https://registry.npmjs.org/ && npm run build"
  }
}

